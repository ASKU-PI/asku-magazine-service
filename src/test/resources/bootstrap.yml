spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
    bus:
      enabled: false
  rabbitmq:
    listener:
      direct:
        auto-startup: false
  # TODO: find a way to prevent the tests from trying to connect to rabbitmq